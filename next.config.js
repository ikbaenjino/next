// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Jika Anda menggunakan gambar dari domain eksternal, tambahkan di sini
    // images: {
    //   domains: ['example.com'],
    // },
  };
  
  module.exports = nextConfig;